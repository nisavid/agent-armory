# Domain Docs

This repo uses a single-context domain-doc layout.

When a skill needs project domain language or architectural decisions, check:

- `CONTEXT.md` at the repo root, if present.
- `docs/adr/`, if present.

If these files do not exist, proceed silently. Do not suggest creating them unless the task is specifically about clarifying domain language or recording a decision.
